{
  "name": "inflow-lab-console",
  "private": true,
  "description": "Lint tooling for the operator console. The console is plain browser ES modules with no build step; this exists only so app/static has the same kind of gate the Python side has.",
  "scripts": {
    "lint": "eslint app/static"
  },
  "devDependencies": {
    "eslint": "^9.18.0",
    "globals": "^15.14.0"
  }
}
